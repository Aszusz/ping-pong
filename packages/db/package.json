{
  "name": "@ping-pong/db",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "drizzle-kit": "drizzle-kit",
    "push": "set -a && . ../../.env && set +a && drizzle-kit push && DATABASE_URL=${TEST_DATABASE_URL:?Missing TEST_DATABASE_URL} drizzle-kit push",
    "generate": "set -a && . ../../.env && set +a && drizzle-kit generate",
    "migrate": "set -a && . ../../.env && set +a && drizzle-kit migrate"
  },
  "dependencies": {
    "drizzle-orm": "^0.39",
    "pg": "^8"
  },
  "devDependencies": {
    "@types/node": "^25.3.5",
    "@types/pg": "^8",
    "drizzle-kit": "^0.31",
    "typescript": "^5"
  }
}
