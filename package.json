{
  "name": "ping-pong",
  "private": true,
  "type": "module",
  "scripts": {
    "docker:up": "docker compose --env-file .env.local up -d && docker compose --env-file .env.local --profile test up -d",
    "docker:down": "docker compose --env-file .env.local down && docker compose --env-file .env.local --profile test down",
    "docker:reset": "docker compose --env-file .env.local down -v && docker compose --env-file .env.local --profile test down -v",
    "db:push": "dotenv -e .env.local -- pnpm --filter @ping-pong/db run push && dotenv -e .env.local -v DATABASE_URL=postgresql://postgres:postgres@localhost:5433/pingpong_test -- pnpm --filter @ping-pong/db run push",
    "web:dev": "pnpm --filter @ping-pong/web run dev",
    "test": "playwright test --config e2e/playwright.config.ts",
    "format": "prettier --write .",
    "lint": "eslint .",
    "lint:fix": "eslint --fix .",
    "typecheck": "pnpm -r run typecheck",
    "qual": "pnpm format && pnpm lint && pnpm typecheck"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@playwright/test": "^1.58.2",
    "dotenv-cli": "^8.0.0",
    "eslint": "^10.0.2",
    "eslint-config-prettier": "^10.1.8",
    "eslint-plugin-react-hooks": "^7.0.1",
    "playwright-bdd": "^8.4.2",
    "prettier": "^3.8.1",
    "prettier-plugin-tailwindcss": "^0.7.2",
    "typescript": "^5.7.3",
    "typescript-eslint": "^8.56.1"
  }
}
